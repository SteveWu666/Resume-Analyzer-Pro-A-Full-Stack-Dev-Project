# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# For local MongoDB (install MongoDB locally):
MONGODB_URI=mongodb://localhost:27017/resume_analyzer

# For MongoDB Atlas (cloud - recommended):
MONGODB_URI=mongodb+srv://nihaosteve000:Wql010420@resumeanalyzer.eybtzoi.mongodb.net/resume_analyzer?retryWrites=true&w=majority&appName=ResumeAnalyzer

# Security - IMPORTANT: Change this to a strong secret (32+ characters)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long-change-this-in-production

# Optional: Default DeepSeek API Key (users can also add their own)
DEFAULT_DEEPSEEK_API_KEY=Put Your Own API Key Here

# Logging Level (optional)
LOG_LEVEL=info

# File Upload Limits
MAX_FILE_SIZE=10485760

# Rate Limiting (requests per 15 minutes)
RATE_LIMIT=100